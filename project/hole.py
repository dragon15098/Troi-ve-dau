import random
class Hole:
    def __init__(self):
        self.list_hole = []

    def check_match(self, x, y):
        for i in range (len(list)):
            if ((x == list[i]["x"]) and (y == list[i]["y"])):
                return False
        return True

    def add_hole(self, weidth, height):
        temp_x = random.randint(0, weidth)
        temp_y = random.randint(0, height)
        if check_match(temp_x, temp_y)
        self.list_hole.append()
